interface:
  display_name: "FirstHome.tools"
  short_description: "Repo-specific patterns and workflows for FirstHome.tools"
  default_prompt: "Use the FirstHome.tools repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true